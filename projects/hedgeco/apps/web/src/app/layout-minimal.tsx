import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "HedgeCo.Net - The Premier Hedge Fund Network",
  description: "Access thousands of dollars worth of data, all completely Free with Investor Registration. 513K+ Alternative Investment Funds, 172+ Investment Professionals, $695M+ Assets Under Management, 82+ Countries Worldwide.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} antialiased bg-hedgeco-light text-hedgeco-text-dark`}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-1">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  );
}